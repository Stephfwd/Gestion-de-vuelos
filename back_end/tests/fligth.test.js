// Este archivo tenía un error de ortografía en el nombre y ha sido reemplazado por flight.test.js
// Puedes borrar este archivo (fligth.test.js) manualmente.
describe('Archivo Duplicado', () => {
  it('debería ser ignorado', () => {
    expect(true).toBe(true);
  });
});
